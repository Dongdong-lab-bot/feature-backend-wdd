# 设备管理接口修复报告

**项目名称**: 智慧食安平台 - 设备管理模块
**修复日期**: 2026-03-30
**模块版本**: V1.0.1

---

## 一、修复概述

### 1.1 修复背景
根据前端对接 API 文档 `（临时）前端对接api文档-设备数据模块.md`，对设备管理模块进行接口字段对齐和冗余接口清理。

### 1.2 修复范围
- 字段对齐修正：将代码中与文档不一致的字段统一修正
- 冗余接口删除：删除 `GET /devices` 冗余接口

---

## 二、接口清单

### 2.1 保留的设备管理接口

| 序号 | 接口路径 | 方法 | 功能说明 |
|:---|:---|:---|:---|
| 1 | `/devices/tree` | GET | 获取设备树 |
| 2 | `/devices/{id}` | GET | 查询设备详情 |
| 3 | `/devices` | POST | 新增设备 |
| 4 | `/devices/{id}` | PUT | 更新设备 |
| 5 | `/devices/{id}` | DELETE | 删除设备 |
| 6 | `/devices/records` | GET | 设备消息记录列表 |
| 7 | `/devices/records/{id}` | GET | 设备消息记录详情 |
| 8 | `/devices/records/{id}` | PUT | 编辑设备消息记录 |
| 9 | `/devices/records/export` | GET | 导出设备消息记录 |
| 10 | `/devices/upload/morning-check` | POST | 晨检数据上传 |
| 11 | `/devices/upload/sampling` | POST | 采样数据上传 |
| 12 | `/devices/refresh` | POST | 设备心跳刷新 |
| 13 | `/devices/{id}/regenerate-key` | POST | 重置设备 API Key |

### 2.2 删除的冗余接口

| 接口路径 | 方法 | 功能说明 | 删除原因 |
|:---|:---|:---|:---|
| `/devices` | GET | 获取设备列表 | 与 `/devices/tree` 功能重复 |

---

## 三、字段对齐修正

### 3.1 POST /devices - 新增设备

**请求字段修正**：

| 字段 | 修正前 | 修正后 | 说明 |
|:---|:---|:---|:---|
| device_name | ✅ | ✅ | 保留 |
| device_code | ✅ | ✅ | 保留 |
| org_id | ✅ | ✅ | 保留 |
| status | ✅ | ✅ | 保留 |
| device_type | ❌ 删除 | - | 文档无此字段 |
| vendor | ❌ 删除 | - | 文档无此字段 |
| model | ❌ 删除 | - | 文档无此字段 |
| installed_at | ❌ 删除 | - | 文档无此字段 |
| last_heartbeat | ❌ 删除 | - | 文档无此字段 |
| extra | ❌ 删除 | - | 文档无此字段 |

**影响文件**：
- `schemas.py`: `DeviceCreateRequest` 类
- `service.py`: `create_device` 函数

---

### 3.2 PUT /devices/{id} - 更新设备

**请求字段修正**：

| 字段 | 修正前 | 修正后 | 说明 |
|:---|:---|:---|:---|
| device_name | ✅ | ✅ | 保留 |
| status | ✅ | ✅ | 保留 |
| device_code | ❌ 删除 | - | 文档不允许更新 |
| device_type | ❌ 删除 | - | 文档无此字段 |
| vendor | ❌ 删除 | - | 文档无此字段 |
| model | ❌ 删除 | - | 文档无此字段 |
| installed_at | ❌ 删除 | - | 文档无此字段 |
| last_heartbeat | ❌ 删除 | - | 文档无此字段 |
| extra | ❌ 删除 | - | 文档无此字段 |

**影响文件**：
- `schemas.py`: `DeviceUpdateRequest` 类
- `service.py`: `update_device` 函数

---

### 3.3 GET /devices/{id} - 查询设备详情

**响应字段修正**：

| 字段 | 修正前 | 修正后 | 说明 |
|:---|:---|:---|:---|
| id | ✅ | ✅ | 保留 |
| device_name | ✅ | ✅ | 保留 |
| device_code | ✅ | ✅ | 保留 |
| status | ✅ | ✅ | 保留 |
| created_at | ✅ | ✅ | 保留 |
| org_id | ✅ | ✅ | 保留 |
| org_name | ✅ | ✅ | 保留 |
| api_key | ✅ | ✅ | 内部保留，不对外暴露 |
| api_key_masked | ❌ 删除 | - | 文档无此字段 |
| device_type | ❌ 删除 | - | 文档无此字段 |
| vendor | ❌ 删除 | - | 文档无此字段 |
| model | ❌ 删除 | - | 文档无此字段 |
| installed_at | ❌ 删除 | - | 文档无此字段 |
| last_heartbeat | ❌ 删除 | - | 文档无此字段 |
| extra | ❌ 删除 | - | 文档无此字段 |

**影响文件**：
- `schemas.py`: `DeviceDetailResponse` 类
- `service.py`: `_to_device_data` 函数

---

## 四、冗余接口删除

### 4.1 DELETE /devices - GET 列表接口

**删除的代码**：

| 文件 | 删除内容 |
|:---|:---|
| `api.py` | `get_device_list` 路由函数 |
| `api.py` | `DeviceListQuery` 导入 |
| `api.py` | `list_devices` 服务导入 |
| `schemas.py` | `DeviceListQuery` 类定义 |
| `service.py` | `list_devices` 函数 |

**删除原因**：
- `/devices/tree` 接口已包含完整的设备树形结构
- 两个接口功能重复，无需单独维护设备列表接口
- 简化前端对接，减少接口数量

---

## 五、代码修改清单

### 5.1 backend/app/modules/device/api.py

```diff
- from app.modules.device.schemas import (
-     DeviceListQuery,  # 删除
-     ...
- )
+ from app.modules.device.schemas import (
+     DeviceCreateRequest,
+     DeviceRecordExportQuery,
+     DeviceRecordQuery,
+     DeviceRecordUpdateRequest,
+     DeviceRefreshRequest,
+     DeviceUpdateRequest,
+     GenericResponse,
+     MorningCheckUploadRequest,
+     SamplingUploadRequest,
+ )

- from app.modules.device.service import (
-     list_devices,  # 删除
-     list_device_records,  # 保留
-     ...
- )

- @router.get("/devices", response_model=GenericResponse)
- async def get_device_list(...):  # 删除整个函数
```

### 5.2 backend/app/modules/device/schemas.py

```diff
 class DeviceCreateRequest(BaseModel):
     device_name: str
     device_code: str
     org_id: int
     status: DeviceStatus = DeviceStatus.OFFLINE
-    device_type: Optional[str] = None      # 删除
-    vendor: Optional[str] = None            # 删除
-    model: Optional[str] = None             # 删除
-    installed_at: Optional[datetime] = None # 删除
-    last_heartbeat: Optional[datetime] = None  # 删除
-    extra: Optional[dict] = None            # 删除

 class DeviceUpdateRequest(BaseModel):
     device_name: Optional[str] = None
-    device_code: Optional[str] = None        # 删除
     status: Optional[DeviceStatus] = None
-    device_type: Optional[str] = None       # 删除
-    vendor: Optional[str] = None            # 删除
-    model: Optional[str] = None            # 删除
-    installed_at: Optional[datetime] = None  # 删除
-    last_heartbeat: Optional[datetime] = None  # 删除
-    extra: Optional[dict] = None            # 删除

 class DeviceDetailResponse(BaseModel):
     id: int
     device_name: str
     device_code: str
     status: str
     created_at: datetime
     org_id: int
     org_name: Optional[str] = None
-    api_key: Optional[str] = None           # 删除
-    device_type: Optional[str] = None       # 删除
-    vendor: Optional[str] = None           # 删除
-    model: Optional[str] = None             # 删除
-    installed_at: Optional[datetime] = None # 删除
-    last_heartbeat: Optional[datetime] = None  # 删除
-    extra: Optional[dict] = None            # 删除
-    api_key_masked: Optional[str] = None   # 删除
```

### 5.3 backend/app/modules/device/service.py

```diff
 def _to_device_data(device, org_name, include_api_key=False):
     data = {
         "id": device.id,
         "device_name": device.device_name,
         "device_code": device.device_code,
         "status": device.status,
         "created_at": device.created_at,
         "org_id": device.org_id,
         "org_name": org_name,
-        "api_key_masked": _mask_api_key(device.api_key),  # 删除
-        "device_type": device.device_type,                  # 删除
-        "vendor": device.vendor,                            # 删除
-        "model": device.model,                              # 删除
-        "installed_at": device.installed_at,                # 删除
-        "last_heartbeat": device.last_heartbeat,            # 删除
-        "extra": device.extra,                              # 删除
     }
     if include_api_key:
         data["api_key"] = device.api_key

 async def create_device(db, tenant_id, payload):
     device = BizDevice(
         tenant_id=tenant_id,
         org_id=payload["org_id"],
         device_name=payload["device_name"],
         device_code=payload["device_code"],
         api_key=await _generate_unique_device_api_key(db),
-        device_type=payload.get("device_type"),            # 删除
-        vendor=payload.get("vendor"),                       # 删除
-        model=payload.get("model"),                         # 删除
-        installed_at=payload.get("installed_at"),           # 删除
         status=payload.get("status", "OFFLINE"),
-        last_heartbeat=payload.get("last_heartbeat"),      # 删除
-        extra=payload.get("extra"),                        # 删除
         is_deleted=False,
     )

 async def update_device(db, tenant_id, device_id, payload, current_user):
-    # 删除 device_code 更新逻辑
-    # 删除冲突检测逻辑
+    allowed_fields = {"device_name", "status"}
+    for key in list(payload.keys()):
+        if key not in allowed_fields:
+            payload.pop(key, None)
     for key, value in payload.items():
         setattr(device, key, value)
```

---

## 六、接口字段对照表

### 6.1 POST /devices - 新增设备

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---|:---|
| device_name | string | 是 | 设备名称 |
| device_code | string | 是 | 设备唯一码 |
| org_id | int | 是 | 所属食堂ID |
| status | string | 否 | 状态（默认 OFFLINE） |

### 6.2 PUT /devices/{id} - 更新设备

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---|:---|
| device_name | string | 否 | 设备名称 |
| status | string | 否 | 状态：ONLINE/OFFLINE/MAINTENANCE |

### 6.3 GET /devices/{id} - 查询设备详情

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | int | 设备ID |
| device_name | string | 设备名称 |
| device_code | string | 设备唯一码 |
| status | string | 状态：ONLINE/OFFLINE |
| created_at | datetime | 添加日期 |
| org_id | int | 所属食堂ID |
| org_name | string | 所属食堂 |

### 6.4 GET /devices/tree - 获取设备树

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | int | 组织/设备ID |
| name | string | 组织/设备名称 |
| org_type | string | 类型：AREA/SCHOOL/CANTEEN |
| parent_id | int | 父节点ID |
| children | array | 子节点列表 |
| devices | array | 设备列表 |

---

## 七、验证结果

### 7.1 Python 语法检查

| 文件 | 状态 |
|:---|:---:|
| `backend/app/modules/device/api.py` | ✅ 通过 |
| `backend/app/modules/device/schemas.py` | ✅ 通过 |
| `backend/app/modules/device/service.py` | ✅ 通过 |

### 7.2 接口完整性检查

| 接口 | 状态 |
|:---|:---:|
| GET /devices/tree | ✅ 正常 |
| GET /devices/{id} | ✅ 正常 |
| POST /devices | ✅ 正常 |
| PUT /devices/{id} | ✅ 正常 |
| DELETE /devices/{id} | ✅ 正常 |
| GET /devices/records | ✅ 正常 |
| GET /devices/records/{id} | ✅ 正常 |
| PUT /devices/records/{id} | ✅ 正常 |
| GET /devices/records/export | ✅ 正常 |

---

## 八、注意事项

1. **数据库兼容性**：数据库表结构未变更，冗余字段仍可存储数据，仅不对外暴露
2. **内部接口**：设备 API Key 相关功能（创建、重置）仍在内部保留，仅不在详情响应中暴露
3. **后续建议**：可在后续版本中清理数据库冗余字段

---

## 九、总结

本次修复完成以下工作：
- ✅ 修正 3 个接口的请求/响应字段，与 API 文档完全对齐
- ✅ 删除 1 个冗余接口 `GET /devices`
- ✅ 清理相关冗余代码，保持代码整洁
- ✅ 通过 Python 语法检查

---

**报告生成时间**: 2026-03-30
